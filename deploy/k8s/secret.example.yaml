# TEMPLATE - copy to `secret.yaml`, fill, then `kubectl apply -f secret.yaml`.
# Do NOT commit a filled-in copy. Real prod should provision these via Sealed
# Secrets / External Secrets / Vault, not raw YAML.
apiVersion: v1
kind: Secret
metadata:
  name: lumi-secrets
  namespace: lumi
type: Opaque
stringData:
  BOT_TOKEN: "REPLACE_ME"
  CLIENT_ID: "REPLACE_ME"
  POSTGRES_PASSWORD: "REPLACE_ME"
  REDIS_PASSWORD: "REPLACE_ME"
  # Shared secret for the dashboard -> worker internal RPC bridge. The worker
  # refuses to start its RPC server without it when NODE_ENV=production, and
  # the dashboard must present the identical value. `openssl rand -hex 32`.
  RPC_INTERNAL_TOKEN: "REPLACE_ME"
  # Dashboard only. Names must match what apps/dashboard/src/lib/env.ts reads.
  # There is no dashboard Deployment in this directory yet (see README) - these
  # are here so the Secret is ready when one is added.
  DISCORD_OAUTH2_CLIENT_SECRET: "REPLACE_ME"
  DASHBOARD_SESSION_SECRET: "REPLACE_ME"
