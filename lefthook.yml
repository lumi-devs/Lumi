pre-commit:
  parallel: true
  commands:
    typecheck:
      glob: "*.{ts,tsx,cts,mts}"
      run: bun run typecheck
    lint:
      glob: "*.{ts,tsx,js,jsx}"
      run: bunx eslint {staged_files} --fix
