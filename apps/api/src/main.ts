// Thin stub until the real api split (Part II, phases S2/S5). For now the api
// entrypoint boots the full worker so a 4-service deployment runs end-to-end.
import "./service-name.js";
import "@ember/worker/main";
