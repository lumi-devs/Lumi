{
  "name": "@lumi/api",
  "version": "2.1.1",
  "license": "Apache-2.0",
  "description": "Thin api entrypoint — boots @lumi/worker until the real service split.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun src/main.ts",
    "dev": "bun --watch src/main.ts"
  },
  "dependencies": {
    "@lumi/worker": "workspace:*"
  }
}
