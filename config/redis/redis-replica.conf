# Read-only replica of the `redis` service. The actual `replicaof` directive
# is passed on the command line so the password resolves at compose-up time.
appendonly yes
maxmemory 128mb
maxmemory-policy noeviction
